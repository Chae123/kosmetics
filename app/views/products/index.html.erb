<h3>Products</h3>

<% if signed_in? && current_user.admin? %>
	<%= link_to 'New Product', new_product_path %>
<% end %>


<div class="container-fluid">
	<div class="row">
		<%= render @products %>
	</div>
</div>